#include <Servo.h>

//still have to hash out some button problems with tape tho
Servo myservo; 
const int buttonPin = 2;


void setup() {
myservo.attach(9);
pinMode(buttonPin, INPUT);

}

void loop() {
  if (digitalRead(buttonPin) == HIGH) {
myservo.write(0);
  } else {
myservo.write(180);
  }
}
